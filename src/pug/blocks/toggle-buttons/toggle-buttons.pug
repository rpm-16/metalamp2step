mixin toggle(arg)
  block variables
    - let objArgs = arg;

    .toggle
        ul.toggle__list
            li.toggle__item
                input.toggle__input(type= "checkbox" checked= objArgs.checked)
                .toggle__label= objArgs.label                
                    